#!/usr/bin/env node

// Load modules

var Yargs = require('yargs');
var Cli = require('../lib/cli');


var argv = Yargs.usage('Usage: $0 -c config.json -l file.log -u url -i broadcastInterval -p usePreviousIndex').argv;
Cli.start(argv);

